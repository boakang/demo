import 'package:flutter/material.dart';

class AppLocales {
  const AppLocales._();

  static const Locale vi = Locale('vi', 'VN');
  static const Locale en = Locale('en', 'US');

  static const List<Locale> supportedLocales = <Locale>[vi, en];
}

